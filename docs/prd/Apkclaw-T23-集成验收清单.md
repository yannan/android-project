# Apkclaw T23 集成验收清单

## 文档定位

- 对应执行计划 **T23**：绑定、接单、执行、历史、设置五块主流程全量回归。
- **T19（模板）暂缓**：验收表中「模板快捷发起」单列标注 **不适用**，不作为阻塞项。
- 验收结论以本表勾选与下方「问题清单」为准；每次会话结束后可将摘要回填至 `docs/prd/Apkclaw-二开-执行计划-TODO.md` 的 T23 备注。

## 环境与版本

| 项 | 填写 |
| --- | --- |
| 验收日期 | |
| APK / Build | （Debug / Release，`versionName`） |
| 设备型号 / Android API | |
| 服务端 | □ 本地 Mock（`127.0.0.1`，见 Device Config） □ 真实网关 URL |
| LLM | □ Debug 本地模型配置 □ 仅远端 `agent/config` □ 无 LLM 直连验证模式 |

说明：Mock HTTP 服务由应用内拉起（默认 `MockRemoteTaskServerManager.baseUrl()`）；真机若无法访问 `127.0.0.1` 上宿主端口，请改用 `adb reverse` 或真实局域网服务端。

## 五块主流程

勾选：**通过 / 失败 / 暂不测（写明原因）**。失败请在文末问题清单登记。

### 1. 绑定（设备网关注册）

| # | 步骤 | 预期 | 结果 |
| --- | --- | --- | --- |
| 1.1 | 设置 → **设备配置**，填写有效 `baseUrl` 与 **绑定码**，保存 | 保存成功 Toast；网关可按配置启用 | |
| 1.2 | 返回首页，查看 **任务收件箱** 卡片 | 已注册：`deviceToken` 非空时副标题为已连接账号摘要；未绑定码或未注册时为断开文案 | |
| 1.3 | 设置 → **设备网关** 开关切换关闭再打开（若适用） | 关闭后轮询停止；打开后与 **设备配置** 一致行为 | |
| 1.4 | 设置 → **设备状态**（点击摘要 Toast） | 展示心跳 / Shizuku 等与文档一致的摘要信息 | |

参考协议：`docs/prd/Apkclaw-客户端服务端-MVP协议.md`（`/api/v1/device/register`、Bearer）。

### 2. 接单（远端任务进入编排）

| # | 步骤 | 预期 | 结果 |
| --- | --- | --- | --- |
| 2.1 | 网关开启且已绑定，服务端（或 Mock）下发一条 **文本任务** | `DeviceTaskGateway` 轮询拉到任务，`TaskDispatcher` 转入 `TaskOrchestrator` | |
| 2.2 | Mock：`Device Config` 注入队列任务或使用页面内 Mock 文案跑协议验证 | 任务状态经网关回传 `RUNNING` → 终态（见 mock 状态日志或服务端） | |
| 2.3 | 网关关闭时下发任务 | 客户端不应错误执行远端队列任务（不接单） | |

### 3. 执行（端到端）

| # | 步骤 | 预期 | 结果 |
| --- | --- | --- | --- |
| 3.1 | 当前默认执行模式（无障碍 / Shizuku）与任务级 `executionMode` 覆盖 | 行为符合 **T11** 决议顺序：任务指定优先，本地默认次之 | |
| 3.2 | 微信通道任务（若测）：需微信 skill | `skillPackageIds` 与 **T15/T16** 装载链路一致；缺失时报错码可查 | |
| 3.3 | 首页 **结束会话**：运行中点击取消 | 任务取消；网关上报 `CANCELLED`（若仍连接）；无幽灵回调 | |
| 3.4 | **T20**：超时 / 重试 / 网关状态上报退避（抽样一条即可） | 与产品设计一致（日志或服务端可见） | |

深度执行矩阵仍以 `docs/prd/Apkclaw-双模式验证记录.md` 为准；本块只做主链路冒烟。

### 4. 历史

| # | 步骤 | 预期 | 结果 |
| --- | --- | --- | --- |
| 4.1 | 首页 **任务历史** 卡片 → 列表页 | 展示近期任务摘要 | |
| 4.2 | 设置 → **设备** 分组第一项 **任务历史** | 进入同一列表 | |
| 4.3 | 点开一条记录 → 详情 | 步骤流、截图路径正确；横滑缩略图与全屏回看可用 | |
| 4.4 | 执行若干步含 `take_screenshot` | `files/task_history/{runId}/` 下 PNG 与 DB 一致（抽样核对） | |

### 5. 设置（入口收敛与诊断）

| # | 步骤 | 预期 | 结果 |
| --- | --- | --- | --- |
| 5.1 | **设备配置 / 网关 / 执行模式** | 修改后首页卡片与网关行为联动 | |
| 5.2 | Release：**模型**手写入口隐藏；误入 `LlmConfigActivity` 提示退出 | 与 **T22** 一致 | |
| 5.3 | Debug：可选进入本地模型配置页 | 配置可读回设置摘要 | |
| 5.4 | **导出诊断**（设置 → Export diagnostics） | 生成并可分享诊断包 | |
| 5.5 | **旧 IM Bot**：迁入 Legacy 设置页 | 主路径仍以设备网关为准（**T21**）；旧入口仍可配置但不抢占主链路 | |
| 5.6 | **模板快捷发起** | **不适用（T19 暂缓）** | N/A |

## 自动化冒烟（研发侧）

在仓库 `ApkClaw` 目录执行：

```bash
./gradlew :app:compileDebugKotlin
```

将命令输出结论记入下方「备注」。

| 执行日期 | compileDebugKotlin |
| --- | --- |
| | □ 通过 □ 失败 |

## 问题清单

| ID | 模块（绑定/接单/执行/历史/设置） | 现象 | 复现步骤 | 严重级别 | 状态 |
| --- | --- | --- | --- | --- | --- |
| T23-I01 | 执行 | `dual_mode_matrix_summary_20260503_213835.md` 中 `screenshot / ACCESSIBILITY` 汇总行为 `SUCCESS`，但 raw status 明确显示 `[截屏{}]：失败`，最终说明为 Android 10 不支持该能力，属于“任务完成”与“用例通过”混淆 | Device Config → 一键跑双模式矩阵（vivo V1838A / Android 10，`allowModeFallback=false`）→ 查看导出的 `dual_mode_matrix_summary_20260503_213835.md` 第 16 行与第 97-101 行 | 中 | 打开 |
| T23-I02 | 执行 | `dual_mode_matrix_summary_20260503_213835.md` 中 `shell / SHIZUKU` 汇总行为 `SUCCESS`，但 raw status 明确显示“当前可用工具中无执行 shell 命令的功能”，说明矩阵汇总未区分能力缺失与任务终态 | Device Config → 一键跑双模式矩阵（vivo V1838A / Android 10，`allowModeFallback=false`）→ 查看导出的 `dual_mode_matrix_summary_20260503_213835.md` 第 21 行与第 146-148 行 | 高 | 打开 |
| T23-I03 | 执行 | 自定义 mock 任务 `mock_1777817569890` 被记录为 `SUCCESS`，最终答案声称“已成功打开计算器并计算出1+1=2”，但步骤明细并未完成 `1 + 1 =` 操作：在计算器内仅执行了 `AC`、`1`、`+`，随后 `获取屏幕信息` 已跳到应用内旧的 Run detail 页面，结论与实际观测不符 | Device Config → Mock 远端任务输入“打开计算器，计算一加一等于几”并运行；事后读取 `task_history` 中 `task_id=mock_1777817569890` 的步骤：第 31-39 步分别是点击 `AC`、`1`、`+`，缺少第二次 `1` 与 `=`；第 43 步屏幕信息已不在计算器，而是 ApkClaw 的 `Run detail` 页面 | 高 | 打开 |

## 验收结论

- □ **通过**：五块主流程均无阻塞缺陷，或与产品确认遗留项可接受。
- □ **带遗留通过**：遗留项见问题清单 ID：________
- □ **不通过**：阻塞项 ID：________

**签字 / 备注**：________________________
