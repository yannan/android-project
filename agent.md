# agent.md

## 目的

本文件用于约束后续 AI 在本仓库中的协作方式，确保 `Apkclaw` 二开任务的执行计划持续一致、可追踪、可交接。

## 必须遵守的规则

1. 每次开始任何与 `Apkclaw` 二开相关的任务前，必须先阅读：
   - `/Users/yannan/docker-apps/android-project/docs/prd/Apkclaw-二开-执行计划-TODO.md`
2. 每次任务结束前，必须更新上述 Todo 文档，至少同步以下内容：
   - 对应任务的 `状态`
   - `最后更新`
   - `备注`
   - `更新记录`
3. 如果本次工作新增了计划外任务，必须先把新任务补充进 Todo 文档，再开始实现。
4. 如果某任务被阻塞，必须把状态改为 `阻塞`，并在备注中写明阻塞原因与缺失依赖。
5. 如果某任务已完成，必须明确对应的交付物或验证结果。
6. 不允许只修改代码而不更新 Todo 文档。

## 任务执行顺序

后续任务原则上按 Todo 文档中的依赖顺序推进。若需跳过顺序，必须在 Todo 文档备注中写明原因。

## 当前唯一任务跟踪文档

- `/Users/yannan/docker-apps/android-project/docs/prd/Apkclaw-二开-执行计划-TODO.md`

## 输出要求

- 汇报进度时，优先引用 Todo 文档中的任务 ID，例如 `T01`、`T02`。
- 完成任务后，说明：
  - 完成了哪些任务 ID
  - 哪些任务状态发生变化
  - 下一步将推进哪个任务 ID
